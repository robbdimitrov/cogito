declare global {
  namespace App {
    // Locals: values set on event.locals in hooks.server.ts
    // PageData: data returned from load functions
    // PageState: client-side page state (history.state)
    // Platform: platform-specific context (Cloudflare, etc.)
  }
}

export {};
