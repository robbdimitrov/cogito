apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: seaweedfs
  labels:
    app: cogito
    component: seaweedfs
spec:
  serviceName: seaweedfs
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: cogito
      component: seaweedfs
  template:
    metadata:
      labels:
        app: cogito
        component: seaweedfs
    spec:
      automountServiceAccountToken: false
      containers:
      - name: seaweedfs
        # Pin to a specific version; verify against running cluster before upgrading.
        image: chrislusf/seaweedfs:3.68
        command: ["/bin/sh", "-ec"]
        args:
        - |
          cat > /etc/seaweed/s3.json <<EOF
          {
            "identities": [
              {
                "name": "cogito",
                "credentials": [
                  {
                    "accessKey": "$(cat /etc/seaweed-secrets/s3-access-key)",
                    "secretKey": "$(cat /etc/seaweed-secrets/s3-secret-key)"
                  }
                ],
                "actions": ["Read", "Write", "List", "Tagging"]
              },
              {
                "name": "cogito-provisioning",
                "credentials": [
                  {
                    "accessKey": "$(cat /etc/seaweed-secrets/s3-provisioning-access-key)",
                    "secretKey": "$(cat /etc/seaweed-secrets/s3-provisioning-secret-key)"
                  }
                ],
                "actions": ["Admin"]
              }
            ]
          }
          EOF
          exec weed server -s3 -s3.config=/etc/seaweed/s3.json
        securityContext:
          allowPrivilegeEscalation: false
        ports:
        - containerPort: 8333
          name: s3
        resources:
          limits:
            memory: 256Mi
          requests:
            memory: 64Mi
            cpu: 50m
        volumeMounts:
        - name: seaweedfs-data
          mountPath: /data
        - name: seaweedfs-config
          mountPath: /etc/seaweed
        - name: seaweedfs-credentials
          mountPath: /etc/seaweed-secrets
          readOnly: true
      volumes:
      - name: seaweedfs-config
        emptyDir:
          medium: Memory
      - name: seaweedfs-credentials
        secret:
          secretName: cogito-db-secret
          items:
          - key: s3-access-key
            path: s3-access-key
          - key: s3-secret-key
            path: s3-secret-key
          - key: s3-provisioning-access-key
            path: s3-provisioning-access-key
          - key: s3-provisioning-secret-key
            path: s3-provisioning-secret-key
  volumeClaimTemplates:
  - metadata:
      name: seaweedfs-data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: seaweedfs
  labels:
    app: cogito
    component: seaweedfs
spec:
  clusterIP: None
  selector:
    app: cogito
    component: seaweedfs
  ports:
  - name: s3
    port: 8333
    targetPort: 8333
